
function max(arr) {
    let result = 0 

    for (let i = 0; i < array.length; i++) {
        const element = array[i];
        
    }
}

function min(arr) {

}

function mean(arr) {
    let result = 0 

    for (let i = 0; i < array.length; i++) {
       result = result + arr[i];
        
    }  
    console.log(result)
    return result / arr.length
}
